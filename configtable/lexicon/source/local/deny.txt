# 本仓自有屏蔽词(优先级最高,归入 local/deny 分类)。
#
# 用途:上游词库覆盖不到的、游戏自身需要挡的词。典型是冒充官方身份的变体
# (GM / 客服 / 管理员 的谐音、拆字、加符号写法)—— 保留词那一层只挡精确子串,
# 变体要在这里补。
#
# 格式:一行一个词,# 开头是注释,空行忽略。
# 归一化与上游同源(NFKC + 折叠),所以这里写半角小写即可,全角 / 大小写变体自动覆盖。
#
# 改完必须重跑导入器,产物才会变:
#   go run ./tools/lexicon-import -source-rev "local@<改动说明>"
