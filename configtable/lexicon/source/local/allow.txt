# 本地白名单:从合并结果中**剔除**的词(误杀修正)。
#
# 上游词库是通用审核词表,拿来判定游戏昵称必然有误杀 —— 常见是把正常游戏用语、
# 职业名、地名判成敏感词。运维发现误杀后在这里加一行,重跑导入器即可解除,
# 不必等上游修、也不许直接改 source/konsheng 或 source/houbb 下的上游快照
# (那两份必须与上游 commit 保持逐字节一致,否则下次同步快照时冲突且无法核对)。
#
# 格式:一行一个词,# 开头是注释,空行忽略。写归一化前的原样即可。
#
# ⚠️ 白名单是**整词剔除**,不是"包含此词就放行":剔除 "菊花" 后,昵称里出现
# "菊花" 不再命中,但更长的命中词若本身仍在库里,照样拦。
