# Pandora Envoy 本地开发期 — 不入库文件
#
# 证书 / 私钥由 mkcert 在本机生成,绝对不能入仓库(违反 AGENTS.md §3 红线)。
# 配置文件 envoy.yaml 入库,证书路径在配置里写死 /etc/envoy/cert.pem + key.pem。

cert.pem
key.pem
*.pem
*.key
*.crt

# 重签时旧证书被改名留档(envoy_cert.ps1:cert.pem -> cert.pem.bad),换了后缀就不再匹配
# 上面的 *.pem/*.key —— 私钥会以 untracked 身份出现在 git status 里,极易被 `git add .` 带进去。
*.bad
