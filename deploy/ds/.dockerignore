# 打包产物 / 大文件不进 docker build context（context 是本目录）。
*.md
# stage/ 是唯一要进镜像的内容，显式保留；其它忽略由上面规则覆盖。

# 注：曾评估把 PandoraServer.debug(258MB) / .sym(131MB) 排除出运行镜像以省约 30% 体积，
# 2026-08-02 按用户决定**保留在镜像内**——当前正处于 DS 崩溃排查期，容器内自带符号
# 才能直接拿到符号化崩溃栈（本轮 Chaos / WorldPartition 卡死的定位全靠它）。
# 若日后转为常态运行且体积成为问题，再评估「归档留符号 + 离线符号化」方案。
